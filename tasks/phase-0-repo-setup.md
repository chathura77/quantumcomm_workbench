# Codex task - Phase 0 repository setup

Read `AGENTS.md`, `docs/ARCHITECTURE.md`, and `docs/ROUTES_AND_PAGES.md`.

Create or verify the base Next.js + TypeScript app. Add Tailwind, test setup, lint/typecheck/build scripts, global layout, navigation, footer disclaimer, and route skeletons for every route in `docs/ROUTES_AND_PAGES.md`.

Do not leave empty placeholders. Every page should have at least useful explanatory content, links to related pages, and a clear MVP status.

Acceptance:

- `npm run typecheck` passes.
- `npm run build` passes.
- Every route renders.
- `README.md` explains how to run the project.
